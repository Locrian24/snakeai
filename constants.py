#!/usr/bin/env python3

class Constants:

    DIMENSION = 30
    PIXEL_SIZE = 10
    SCREEN_SIZE = DIMENSION * PIXEL_SIZE
    VELOCITY = PIXEL_SIZE
    FPS = 30
    DIRECTIONS = [ (0,-1), (1,0), (0,1), (-1, 0) ]
